%main.container.general-main-wrap
  .text-center
    %h1.mb-30 Forgot your password?
    %p.general-password-text We'll email you instructions on how to reset it
  = simple_form_for(resource, as: resource_name,
    url: password_path(resource_name),
    html: { class: 'general-form' }) do |f|
    = f.input :email,
      label: false,
      autofocus: true,
      placeholder: 'Enter your email',
      wrapper_html: { class: 'mb-25' }
    = f.button :submit, 'Email Instructions',
      class: 'btn btn-default center-block mb-20'
    .text-center
      %a.in-gold-500.mb-20{href: "#"} Cancel